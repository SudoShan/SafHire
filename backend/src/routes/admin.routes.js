// admin.routes.js — legacy file superseded by superAdmin.routes.js
// Kept as a no-op to avoid import errors if referenced elsewhere
const express = require('express');
const router = express.Router();
module.exports = router;
